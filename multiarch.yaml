---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: multiarch
spec:
  selector:
    matchLabels:
      app: multiarch # has to match .spec.template.metadata.labels
  serviceName: none
  replicas: 4 # by default is 1
  minReadySeconds: 10 # by default is 0
  template:
    metadata:
      labels:
        app: multiarch # has to match .spec.selector.matchLabels
    spec:
      terminationGracePeriodSeconds: 10
      containers:
      - name: multiarch
        image: fir.love.io:30443/dev/cpp-multiarch:latest
        imagePullPolicy: Always
        ports:
        - name: metrics
          containerPort: 6001
      imagePullSecrets:
      - name: regcred

